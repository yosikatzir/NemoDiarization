# NeMo Diarization - Fully Tested Requirements for Windows CPU-only
# Python 3.11.0+
# Tested on: Windows 10/11 64-bit
# Date: December 2025
# NeMo Version: 2.5.3 (latest stable)

# ============================================================================
# INSTALLATION INSTRUCTIONS
# ============================================================================
#
# Step 1: Install PyTorch CPU version FIRST (MANDATORY):
#   pip install torch==2.5.1 torchvision==0.20.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cpu
#
# Step 2: Install NeMo and other dependencies:
#   pip install -r requirements_tested.txt
#
# ============================================================================

# Core NeMo Toolkit with ASR support
nemo-toolkit[asr]==2.5.3

# Configuration Management
omegaconf==2.3.0
hydra-core==1.3.2

# Audio Processing Libraries
soundfile==0.12.1
librosa==0.10.2.post1
resampy==0.4.3

# NumPy - CRITICAL: Must be <2.0 for NeMo compatibility
numpy>=1.24.0,<2.0.0

# PyTorch Lightning (required by NeMo)
pytorch-lightning==2.4.0
torchmetrics>=0.11.0

# Scientific Computing
scipy==1.13.1

# Performance and Optimization
numba==0.60.0
llvmlite>=0.42.0

# Text Processing
text-unidecode==1.3
regex

# Progress Bars and UI
tqdm==4.66.5

# YAML and Configuration
pyyaml==6.0.2

# Data Loading and Processing
webdataset==0.2.86
pandas==2.2.3

# Diarization-Specific
pyannote.metrics==3.2.1
pyannote.core>=5.0.0
pyannote.database>=5.0.0

# Clustering and Machine Learning
scikit-learn==1.5.2

# Visualization (optional, for debugging)
matplotlib==3.9.2

# Audio Feature Extraction
speechbrain==1.0.2

# Utilities
filelock>=3.12.0
packaging
typing-extensions>=4.10.0

# JSON and Data Handling
jsonlines
inflect

# Additional ASR Dependencies
editdistance
jiwer

# Kaldi Compatibility (optional)
# kaldiio==2.18.0  # Uncomment if you need Kaldi file format support

# ==========================================================================
# NOTES:
# ==========================================================================
# 1. PyTorch CPU installation is separate (see Step 1 above)
# 2. Total installation size: ~1.5GB
# 3. First run will download models (~120MB) - cached for subsequent runs
# 4. Models cached in: C:\Users\YourUsername\.cache\torch\NeMo\
# 5. For faster CPU inference, consider using smaller models in config
# ==========================================================================

# Version Information:
# - Tested with Python: 3.11.14
# - NeMo Toolkit: 2.5.3
# - PyTorch: 2.5.1+cpu
# - Platform: Windows 10/11 (64-bit)
# - Date: December 1, 2025
